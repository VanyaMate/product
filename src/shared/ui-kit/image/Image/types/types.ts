export enum ImageRadiusType {
    ZERO   = 'zero',
    SMALL  = 'small',
    MEDIUM = 'medium',
    LARGE  = 'large',
}

export enum ImageBorderType {
    NONE   = 'none',
    SOLID  = 'solid',
    DOTTED = 'dotted',
}